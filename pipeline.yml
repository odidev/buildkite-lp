env:
  DOCKER_USERNAME: "abhay900"
  DOCKER_PASSWORD: "dckr_pat_d5J65dFjKrITnOREWZR1dWEZA7U"

steps:
  - label: "Build and push multi-arch image"
    commands:
      - docker buildx rm mybuilder || true
      - docker buildx create --use --name mybuilder
      - docker buildx inspect --bootstrap
      - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
      - docker buildx build --platform linux/amd64,linux/arm64 -t abhay900/multi-arch-app:latest --push .
    agents:
      queue: buildkite-queue
